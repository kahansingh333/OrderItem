apiVersion: v1
kind: Service
metadata:
    name: kahansinghtruyumwebapiorderitem
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: kahansinghtruyumwebapiorderitem